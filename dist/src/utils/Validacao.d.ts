export declare class Validacao {
    private static RegexEmailValido;
    private static RegexTelefoneValido;
    static emailValido(email: string): boolean;
    static telefoneValido(telefone: string): boolean;
}
